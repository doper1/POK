add_users_to_group_whatsapp_api_wev:
  build:
    context: ./add_users_to_group_whatsapp_api/web
    dockerfile: Dockerfile
  container_name: pok
  restart: always
  volumes:
    - whatsapp_session_add_group:/var/lib/wa/add_group
  ports:
    - '3000:3000'
